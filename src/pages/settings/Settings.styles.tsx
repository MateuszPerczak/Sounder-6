import styled from "@emotion/styled";
import { motion } from "framer-motion";

const StyledSettings = styled(motion.section)`
  display: flex;
  flex-direction: column;
  flex: 1;
  gap: 10px;
  padding-right: 2px;
`;

export default StyledSettings;
